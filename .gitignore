# Terraform state (remote state stored in Artifactory; local files are bootstrap only)
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl
state/
bootstrap/.terraform/
bootstrap/terraform.tfstate*

# Variable files with secrets
*.tfvars
!*.tfvars.example

# Crash logs
crash.log
crash.*.log

# Override files (local dev only)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# IDE
.idea/
.vscode/
*.swp
*.swo

# Secrets
.env
.env.*
!.env.example
.secrets

#suggesitons and tests
suggestions.txt